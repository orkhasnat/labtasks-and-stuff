#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        deque<int> a;
        int n, tmp,s1,c1=0,c2=0;
        cin >> n>>s1;
        int tb = n;
        while (tb--)
        {
            cin >> tmp;
            a.push_back(tmp);
        }
        while(a.size()!=0){
        int s2=0;
        while()

        }
    }
}